# Dataset — never commit (lives in ../FinalData or Kaggle dataset mount).
# Anchored to repo root with a leading slash so it does NOT match the source
# package src/web_agent/data/.
/data/
*.json.zip
/images/

# Checkpoints + artifacts
/checkpoints/
/kaggle_outputs/
/artifacts/
/outputs/
/locked_benchmark_mount/
*.ckpt
*.pt
*.pth
*.safetensors

# Table 2 browser/runtime evidence (raw and never committed)
/browser_profiles/
/captures/
/private/
/playwright/.auth/
*.har
*.webm
*.mp4
*.mkv
*.trace
*.trace.zip
**/runtime/screenshots/
*.faiss
*.index
*.embeddings.npy
embeddings.npy
/playwright-report/
/test-results/
cookies*.json
credentials*.json
storage_state*.json

# Generated results. Table 2 exports stay ignored until a separately reviewed,
# redacted paper artifact is deliberately placed in a tracked paper location.
results/curves/
results/figures/
results/table2/
wandb/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ipynb_checkpoints/

# Env
.venv/
venv/
.env
.env.*
!.env.example
!.env.template

# local data dumps - never commit
FinalData.zip
*.zip
FinalData/

# Ignore new local study-note additions; existing indexed legacy notes are preserved.
learn/

# Exception: model-comparison run artifacts are tracked via Git LFS.
# Must come after the *.ckpt rule above to re-include them.
!/webagent_comparison/**/*.ckpt
