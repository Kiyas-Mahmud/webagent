# CLAUDE.md

**ALWAYS read `project_progress.md` (repo root) FIRST — before planning or writing any code.** It is the live build log: what's done, decisions, current state, gotchas. Update it after every task.

Then read `docs/AGENT.md` — the entry point with all context and operating rules. Follow it.

For Table 2 runtime, post-training handoff, recovery budgets, memory timing,
and browser evaluation, read
`docs/TABLE2_END_TO_END_RUNTIME_AND_POST_TRAINING_PLAN.md` next. It is the
canonical authority for that workstream and supersedes conflicting legacy
runtime statements in the general architecture/progress documents. Those
older documents remain authoritative only for unaffected offline training
history and architecture context.

All design docs live in `docs/`: `AGENT.md`, `PROJECT_SPECIFICATION.md`, `ARCHITECTURE_DESIGN.md`, `MODEL_TRAINING_PLAN.md`, `IMPLEMENTATION_PLAN.md`.
