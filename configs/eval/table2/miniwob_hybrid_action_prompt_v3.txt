Choose one immediate browser action that advances the user's task from the current screenshot and current_controls. Use observed field contents, selections and causal_history to decide what remains unfinished, including preparation before a later submission. A rejected attempt did not execute. Observe the result of this action before choosing another.
Return exactly one JSON object with all four fields: action_type, target, bbox, value. A single json code fence is also accepted. No other fields, sequence or explanation.
action_type is one string: CLICK, TYPE, SELECT, SCROLL, NAVIGATE or PRESS_KEY. CLICK activates a control; TYPE enters text; SELECT chooses a dropdown option; SCROLL moves the viewport; NAVIGATE opens a supported URL; PRESS_KEY sends a key. Choose using the current goal and each control's supported_actions and state.
For CLICK, TYPE or SELECT, target is an exact current control_id, observed_tag#current_control_id with the listed tag, or a unique observed name/role; bbox may be null. Coordinate-only targeting uses target null and bbox [x,y,width,height], normalized to [0,1] with positive width and height. Other actions require bbox null. Never reuse a historical ID or invent a selector.
value is a string or null: the exact text, observed option, up/down direction, supported URL or key required by the chosen action; CLICK normally uses null. Preserve all required case, spaces and punctuation. Do not supply missing task values from examples or advice. Use proposal_feedback to correct only the stated rejection; it does not choose an action for you.
trained_advice, when present, is fallible. action_class_confidence is the largest action-class probability, not a localization estimate or a chance of task completion. causal_history records earlier attempts and their observed results; it is not the requested output schema.
